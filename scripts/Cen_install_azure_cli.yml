steps:
  - script: |
      curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash         # Download and install the Azure CLI on the agent machine using a script from aka.ms
    displayName: 'Install Azure CLI'                                 # This step installs the Azure CLI and is displayed as 'Install Azure CLI' in the pipeline